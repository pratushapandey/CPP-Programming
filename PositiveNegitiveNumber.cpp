#include<bits/stdc++.h>
using namespace std;

int main(){
	int num;
	
	cout<<"Enter the number : ";
	cin >> num;
	
	if(num==0)
	cout<<"Zero"; else(num > 0) ? cout << "Positive Number" : cout <<"Negitive Number";
	
	return 0;
}
